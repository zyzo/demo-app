export const initialState = {
  displayList: [
    {
      id : 1,
      name: 'Jasd',
      amount: 3.3
    }
  ]
};

const recipes = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default recipes;
