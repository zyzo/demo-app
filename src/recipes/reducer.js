export const initialState = {
  displayList: [
    {
      id : 1,
      name: 'Jasd',
      amount: 3.3
    },
    {
      id: 2,
      name: 'AZEAZE',
      amount: 213.2
    }
  ]
};

const recipes = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default recipes;
